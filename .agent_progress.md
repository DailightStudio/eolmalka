# 진행 중: 스토어 런칭 (Firebase 정체성 복구 → 서명 누출 정리 → 빌드)

## 결정: 번들ID = com.dailightstudio.eolmalka (Path 2)
프로젝트가 반쪽 이주 상태였음: App Store 앱(ASC 6782645385)·eas.json 제출타깃·원래 정체성은 **com.dailightstudio**인데, Firebase 설정·app.json·6월 빌드는 **com.jaylabs**로 바뀌어 있었음(207ad24에서 Firebase, cbd75b9에서 app.json). 사용자가 **com.dailightstudio로 통일(Path 2)** 선택.

## 완료
- ☑ 스크린샷 11장 → ASC(6782645385) v1.0 en-US 업로드 완료 (아이폰67 6장 + 아이패드129 5장). 스크립트: asc_shots.mjs(untracked, .p8 경로 참조)
- ☑ app.json 번들ID/package → com.dailightstudio 복원, 커밋 fe3bc44 푸시
- ☑ FX 관련 이전 작업 모두 끝(e48b405 매매기준율 버그+OTA, 320f241 예측 baseline)
- ☑ **(07-23, 맥)** Firebase iOS 재발급본 교체 — `GoogleService-Info.plist` BUNDLE_ID=com.dailightstudio.eolmalka. 커밋 `b6fea6e`. 옛 파일 대비 BUNDLE_ID·GOOGLE_APP_ID만 변경, PROJECT_ID/API_KEY/GCM_SENDER_ID 동일 확인.
- ☑ **(07-23, 맥)** Firebase Android 재발급본 교체 — `google-services.json` 루트 + `android/app/` 2곳. 커밋 `1505dd5`. client 2개(dailightstudio 신규 + jaylabs 기존) 포함이라 applicationId 불일치여도 플러그인 매칭 성공.

## 07-23 맥에서 발견한 문제 2건 (사용자 "다 해" 승인)
### A. 공개 레포에 키스토어 비번 평문 커밋 ★보안
- 레포 `DailightStudio/eolmalka`는 **public**(`private:false` 확인).
- `android/app/build.gradle:109-111`에 `storePassword`(32자)·`keyAlias`(8자)·`keyPassword`(32자) **리터럴**.
  `plugins/withAndroidSigning.js`가 env로 주입한 prebuild 산출물이 그대로 커밋됨.
- 유입 커밋: `7cadf6a`(build: Android native project prebuild 추가), `40b1b79`.
- **`.keystore` 파일 자체는 미커밋**(추적 파일 0) → 비번만으로는 서명 불가. 위험도 낮으나 히스토리에 잔존.
- Android는 아직 Play 미출시(빌드가 6월부터 계속 실패) → **키 교체 비용이 지금 가장 쌈**.

### B. 커밋된 `android/`가 반쪽 — Java/Kotlin 소스 0개
- 추적 41파일이 gradle·manifest·리소스뿐. `MainActivity`/`MainApplication` **없음**.
- `.gitignore`에 `/android/` 있으나 추적이 우선 → `git check-ignore` "not ignored" → **EAS가 bare 워크플로로 판정 → prebuild 건너뛰고 불완전 프로젝트에 gradle 실행**.
- 6월부터의 gradlew ERRORED 최유력 후보(단, 실제 Gradle 로그 미확인 → 미확정).
- Windows Docker 경로는 ENTRYPOINT가 `expo prebuild --clean --platform android`라 영향 없음(이미 매번 재생성).

## 이 맥(sub)의 환경 실측 (07-23)
- EAS CLI **미설치**, JDK **없음**(keytool 실행 불가), Xcode **없음**(CLT만 — EAS 클라우드 빌드는 무관), `node_modules` **없음**, node v25.9.0 / npm 11.12.1, brew 6.0.2 있음.
- `server-agent/.env`에 EXPO_TOKEN·키스토어 관련 변수 **없음** → `eas login` 비대화형 불가.
- `credentials/`·로컬 keystore 파일 **없음** (키스토어는 Windows 쪽에 있음).
- **eas.json `submit.production` 경로가 전부 Windows 절대경로** (`C:/Users/Jay-server/...` — ascApiKeyPath, serviceAccountKeyPath) → 맥에서 `eas submit` 그대로는 실패.

## 단계
- ☑ 1. android/ 추적 해제 — 커밋 `17b21c7` 푸시. 41파일/882줄 삭제. HEAD에서 평문 비번 제거 확인.
- ☑ 2. 툴체인 설치(맥) — `npm install` 923패키지, `eas-cli 21.1.0`, `openjdk 26`(brew). **Android SDK는 없음 → 로컬 gradle 빌드 불가**(설치 시 수 GB).
- ☑ 3. **문제 B 실증 완료** — `npx expo prebuild --clean --platform android` 돌린 결과:
  - `MainActivity.kt`/`MainApplication.kt`가 `com/dailightstudio/eolmalka/` 아래 **생성됨** (커밋본에는 0개였음 → 커밋된 android/가 불완전했다는 직접 증거)
  - `applicationId`·`namespace` 자동으로 `com.dailightstudio.eolmalka` 복구
  - release 서명 블록 **미주입**(env 없어 플러그인 no-op) → 평문 비번 재발 없음. 남은 `'android'` 값은 디버그 키스토어 공개 기본값
  - `google-services.json` 자동 배치, withAndroidMemory jvmargs 반영 확인
  - `git status` 클린 — android/ 재추적 안 됨
- ☑ 4a. **신규 릴리스 키스토어 생성** — `~/Desktop/eolmalka-keystore-new/` (**레포 바깥**, 700/600).
  `eolmalka.keystore` (PKCS12, RSA2048, 10000일), alias `eolmalka`, 비번 32자 랜덤.
  비번·설치법은 같은 폴더 `CREDENTIALS.txt`. SHA-256 지문 `9E:00:6C:52:...:52:38`.
- ☑ 4b. EXPO_TOKEN 확보 → `eas whoami` = **jaylabs** (wjs9280@gmail.com), app.json owner와 일치.
  토큰은 `server-agent/.env`(600, gitignore)에 `EXPO_TOKEN=`로 보관. **레포에 넣지 말 것.**
- ☑ 5. **빌드 이력 조회로 Play 업로드 리스크 해소**
  - Android 27건: ERRORED 25 / CANCELED 1 / **FINISHED 1**. 유일 성공은 `c527b6fa`
    (2026-06-03, **preview 프로파일 / distribution=INTERNAL / v0.1.0 / APK**).
    production(app-bundle=AAB)은 **한 건도 성공 없음** → 신규 앱은 AAB만 받는 Play에 올라갔을 수 없음
    → **키스토어 교체 안전**(Play Console 직접 확인은 아직 안 함).
  - iOS 7건: FINISHED 2 (2026-06-21 build 27, 2026-06-27 build 28) / ERRORED 5.
- ☑ 6. **iOS 자격증명 벽 없음 확인** — EAS에 `com.dailightstudio.eolmalka`용 배포 인증서
  (Serial 69C051FAC83D0C4B2C9E5F6F6C228EE)·프로비저닝 프로파일(BQ5UC97WJ5) 이미 등록됨.
  Apple Team HB85X53L9D (Jinho SEO, Individual), 둘 다 2027-06-20 만료.
  **비대화형으로 빌드 가능** — 기존 기록의 "Windows 비대화형은 인증서벽으로 불가"는 현재 사실이 아님.
- ☑ 7. **빌드 결과: 둘 다 성공** (git commit `77150ba` 기준)
  - **iOS** `3ade7722` FINISHED — v1.0.0 **build 29**, STORE, SDK 54, 6분 3초.
    IPA: `https://expo.dev/artifacts/eas/w_RfXk4HwWgyYlqu5IPNNXc-Tn46cKdbGoCQ_h05QZ4.ipa`
  - **Android** `2e29e823` FINISHED — v1.0.0 **versionCode 12**, STORE, AAB, 9분 55초.
    AAB: `https://expo.dev/artifacts/eas/6tF5TwXGSqTdj-rtGlyfVQQ-hLEbq5tuNMBFpNeyl1w.aab`
  - **★ Android production(AAB) 성공은 프로젝트 사상 최초.** 6월 이후 25연속 ERRORED가 끊김.
    → **문제 B(커밋된 반쪽 android/) 진단이 실측으로 확정됨.** 고친 것은 `17b21c7`(추적 해제) 하나뿐.
  - 누적: ANDROID {FINISHED 2, ERRORED 25, CANCELED 1} / IOS {FINISHED 3, ERRORED 5}
- ☐ 8. 제출 — **`eas submit -p ios` 실패: `C:/Users/Jay-server/.ssh/AuthKey_WGX6S9GS9Y_ce8f24.p8` 없음**.
  맥 전체 검색 결과 `.p8`·`pindog-*.json` 둘 다 이 맥에 없음(Windows 전용).
  해법: 파일을 맥으로 옮기고 `EXPO_ASC_API_KEY_PATH`/`EXPO_ASC_API_KEY_ID`/`EXPO_ASC_ISSUER_ID`
  환경변수로 넘기면 eas.json 수정 없이 통과(= Windows 설정 안 깨짐).

## ☑ 서명 경로 EAS 일원화 완료 (커밋 `44019ad`)
- 전에는 두 갈래였음: EAS 원격 키스토어(`s-FUk0MDJt`) vs Windows Docker 로컬
  키스토어(`credentials/eolmalka.keystore`). **서로 다른 키** → 번갈아 올리면 Play가 서명 불일치로 거부.
- 조치: `plugins/withAndroidSigning.js` 삭제, `app.json` plugins에서 제거,
  `Dockerfile` ENTRYPOINT의 키스토어 복사 제거.
- **스토어용 서명 AAB는 이제 EAS로만** (`eas build -p android --profile production`).
  Docker 경로는 릴리스 빌드 컴파일 검증용으로 남되 산출물은 디버그 키 서명 → Play 업로드 불가
  (조용히 잘못된 키로 서명되는 것보다 실패가 낫다).
- 검증: env(EOLMALKA_KEYSTORE_PASSWORD 등)를 넣고 prebuild를 돌려도 `signingConfigs.release` 미생성 확인.
- → 4a에서 만든 `~/Desktop/eolmalka-keystore-new/`는 **불필요해짐. 폐기 가능.**

## 히스토리에 남은 평문 비번 — 현재 상태
- HEAD에서는 제거됨(`17b21c7`). 이제 그 비번이 지키던 로컬 키스토어를 **어떤 빌드 경로도 쓰지 않음**(`44019ad`)
  → 실질 위험 소멸. 키스토어 파일 자체는 애초에 미커밋.
- 히스토리(`7cadf6a`, `40b1b79`)에는 여전히 남아 있음. 완전 삭제하려면 `git filter-repo`/BFG로
  **히스토리 재작성 + force push**가 필요한데, public 레포 강제푸시라 Windows 클론이 깨진다
  (re-clone 또는 hard reset 필요). **사장님 명시 승인 없이는 안 함.**

## 남은 블로커
1. **제출용 자격증명 파일이 맥에 없음** — `.p8`(ASC API 키), Play 서비스계정 json 둘 다 Windows 전용 경로.
   맥으로 옮기고 `EXPO_ASC_API_KEY_PATH`/`EXPO_ASC_API_KEY_ID`/`EXPO_ASC_ISSUER_ID` 환경변수로 넘기면
   eas.json 수정 없이 통과. Play 서비스계정은 EAS에 업로드해두면(`eas credentials -p android`) 경로 의존 자체가 사라짐.
   - ASC API 키 발급: https://appstoreconnect.apple.com/access/integrations/api (Users and Access → Integrations, Admin 역할)
     ※ .p8 은 **최초 1회만 다운로드** 가능 → 기존 WGX6S9GS9Y 파일이 Windows에 있으면 그걸 옮기는 게 맞음.
   - Play Console 앱 생성: https://play.google.com/console (Create app)
   - Play 서비스계정 키 발급 가이드: https://expo.fyi/creating-google-service-account
   - 첫 릴리스 수동 업로드는 **불필요** — `eas submit`이 internal testing 트랙에 첫 릴리스를 만들어줌.
2. **[사용자·ASC 웹] iOS 메타데이터/심사제출** — 스크린샷은 올라감. 설명·키워드·App Privacy 입력 후 제출.
3. **[미정] Android 스토어 이미지·Play 리스팅** — iOS 확정 후.

## ☑ 제출 단계 (07-23)
- **iOS 업로드 성공** — build 29 → ASC 6782645385 (submission `3e0068e8`). Apple 처리 완료 = `VALID`.
- **Android 제출 실패 — Google 정책** — 서비스계정 인증은 통과(`eas-deploy@pindog.iam.gserviceaccount.com`)했으나
  `"The first submission of the app needs to be performed manually."`
  → **첫 AAB는 Play Console에서 사람이 직접 올려야 한다.** (Expo 문서에는 자동 가능하다고 쓰여 있으나 실제로는 거부됨 — 문서보다 실측 우선)
  AAB 로컬 사본: `~/Desktop/eolmalka-release/eolmalka-1.0.0-vc12.aab` (40MB)
- **제출 자격증명 경로 상대화** (`bc9c659`) — `./credentials/` 로 통일. Windows도 같은 위치에 두 파일 복사 필요.

## ☑ ASC 메타데이터 API로 채움 (`44e0bec`, `a1df5a6`)
`scripts/asc.mjs` = 의존성 없는 ASC API 클라이언트(node 내장 crypto ES256 JWT, JOSE raw 서명).
- ☑ build 29 → 버전 1.0 연결
- ☑ 설명(1005자)·키워드(88/100)·지원URL·마케팅URL
- ☑ 부제(30/30)·개인정보처리방침 URL(`eolmalka-pages/privacy.html`, 200 확인)
- ☑ 카테고리 FINANCE / UTILITIES
- ☑ 연령등급 설문 — `advertising:true`(AdMob 실탑재)만 true, 나머지 NONE/false.
  필드 타입 함정: `gunsOrOtherWeapons`=enum, `healthOrWellnessTopics`=boolean, `ageAssurance`=boolean(필수).
- ☑ 스크린샷 2세트 기존 업로드분 확인(APP_IPHONE_67, APP_IPAD_PRO_3GEN_129)
- 설명에서 **항공권은 제외** — README 기준 아직 합성 데이터라 허위표시 방지.

## 남은 일 — 전부 사장님 손이 필요 (API로 불가)
1. **Play Console 앱 생성 + 첫 AAB 수동 업로드** — https://play.google.com/console
   이후부터는 `eas submit -p android`가 자동으로 됨.
2. **App Privacy(데이터 수집 선언)** — ASC API가 엔드포인트 미노출(`/v1/appDataUsages` 404).
   ASC 웹에서 직접. Firebase Analytics·Crashlytics·AdMob → 식별자/사용 데이터 수집 해당.
3. **심사 연락처 전화번호** — `contactPhone` 필수인데 레포 어디에도 없음.
   `node scripts/asc-review-detail.mjs "+82 10-XXXX-XXXX"` 한 줄이면 끝.
4. **contentRightsDeclaration** — 뉴스 헤드라인(Google News RSS) 표시 → 제3자 콘텐츠 여부 개발자 판단 필요.
5. **연령등급 선언 눈으로 확인** — Apple에 하는 공식 선언이므로 제출 전 ASC 웹에서 검토 권장.

## 마지막으로 한 것
- ASC 메타데이터 자동 입력 완료. Android는 Google 첫-업로드 정책에 막힘.

## 다음 할 것
- 위 5개 중 1~3 처리되면 iOS 심사 제출 가능.

## 주의
- Firebase 설정파일은 손으로 번들ID만 바꾸면 안 됨(GOOGLE_APP_ID 등이 앱별 발급값 → 콘솔에서 정식 재발급만 유효).
- 봇 자기kill 금지.
- 레포가 **public**이다. 비밀값 커밋 금지.
