# 진행 중: 스토어 런칭 (최신본 빌드 + 스토어 이미지)

## 결정: 번들ID = com.dailightstudio.eolmalka (Path 2)
프로젝트가 반쪽 이주 상태였음: App Store 앱(ASC 6782645385)·eas.json 제출타깃·원래 정체성은 **com.dailightstudio**인데, Firebase 설정·app.json·6월 빌드는 **com.jaylabs**로 바뀌어 있었음(207ad24에서 Firebase, cbd75b9에서 app.json). 사용자가 **com.dailightstudio로 통일(Path 2)** 선택.

## 완료
- ☑ 스크린샷 11장 → ASC(6782645385) v1.0 en-US 업로드 완료 (아이폰67 6장 + 아이패드129 5장). 스크립트: asc_shots.mjs(untracked, .p8 경로 참조)
- ☑ app.json 번들ID/package → com.dailightstudio 복원, 커밋 fe3bc44 푸시
- ☑ FX 관련 이전 작업 모두 끝(e48b405 매매기준율 버그+OTA, 320f241 예측 baseline)

## 남은 일 (블로커)
- ☐ **[사용자] Firebase 콘솔에서 com.dailightstudio용 설정 재발급** — 현재 GoogleService-Info.plist / google-services.json / android/app/google-services.json 은 com.jaylabs. 콘솔에서 com.dailightstudio.eolmalka iOS+Android 앱 등록→다운로드→3파일 교체→커밋. (파일 주면 내가 넣음). git에 옛 com.dailightstudio Firebase설정 없음(복원 불가).
- ☐ **[사용자·맥] iOS 빌드+제출**: git pull → `eas build -p ios --profile production`(Apple 인터랙티브 로그인=인증서 벽 통과) → `eas submit -p ios --profile production`(ascAppId 6782645385). Windows 비대화형은 인증서벽으로 불가.
- ☐ **[진단필요] Android 빌드**: 6월부터 gradlew 단계에서 계속 ERRORED(정체성 무관). memory 플러그인(withAndroidMemory: -Xmx3072m/Metaspace2048m) 있는데도 실패. **실제 Gradle 로그 필요** — 빌드페이지 빨간에러 or 재빌드 로그 확인. 최근 시도 50453910(com.jaylabs) errored.
- ☐ **[사용자·ASC 웹] iOS 메타데이터/심사제출**: 스크린샷은 올라감. 설명·키워드·App Privacy 답 입력 후 "심사 제출".
- ☐ **[미정] Android 스토어 이미지·Play 리스팅**: iOS 확정 후. Play 서비스계정 키 있음(pindog-...json).

## 주의
- Firebase 설정파일은 손으로 번들ID만 바꾸면 안 됨(GOOGLE_APP_ID 등이 앱별 발급값 → 콘솔에서 정식 재발급만 유효).
- 봇 자기kill 금지.
