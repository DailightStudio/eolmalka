node_modules/
.expo/
dist/
web-build/

# EAS
.eas/

# iOS & Android native (managed/CNG — generated at build time)
/android/
/ios/

# Native
*.orig.*
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision

# Android release signing (절대 커밋 금지 — 비번 분실 시 앱 업데이트 불가)
/credentials/

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# env
.env*
!.env.example

# 로컬 키 등록 스크립트 (API 키 평문 포함 — 절대 커밋 금지)
register-env.bat

# typescript
*.tsbuildinfo
expo-env.d.ts

# 캘리브레이션 스크립트 (실데이터 스윕용, 커밋 대상 아님)
scripts/calibrate-*.mjs
scripts/_calibrate_result.json
